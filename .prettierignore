# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/
/drizzle/

# Generated
/src/lib/paraglide/
# `npm run generate:zero` output. drizzle-zero already runs prettier over it (`generate --format`),
# so reformatting it here only fights the generator on every regeneration.
/src/lib/zero/zero-schema.gen.ts
# `npm run generate:emails` output. Reformatting it would fight the generator on every run,
# and the whitespace is load-bearing: mail clients collapse and re-wrap markup differently.
/emails/

# Installed agent skills
/.agents/

# Agent worktrees: whole checkouts of this repo, so every file in them shows up twice.
/.claude/
